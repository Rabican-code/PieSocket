import menu from "./menu"

const menu = () => {
    const [menu] = menu;
    {
        menu.map((item, index) => { 
            7const [submenu] = item;
            {
                submenu.map(() => {
                
            })} 
        });
    }
}